{
    "routes": [
        {
            "route": "/foo",
            "rewrite": "/next.svg"
        },
                {
            "route": "/bar",
            "redirect": "/"
        }
    ],
    "globalHeaders": {
        "cache-control": "max-age=60"
    },
    "responseOverrides": {
        "404": {
          "rewrite": "/notfound"
        }
    }
}
